# ----------------------------------------------------------------------
# VS Code (Visual Studio Code)
# ----------------------------------------------------------------------

# Local workspace settings
# NOTE: If you want to share launch configurations or shared project
# settings (like recommended extensions), you might keep files like
# .vscode/launch.json or .vscode/settings.json.
# However, usually, ignoring the whole folder is the safest default.
.vscode/

# Ignore the workspace storage folder (used for extensions, state, etc.)
.vscode-test/

# ----------------------------------------------------------------------
# macOS (Operating System Files)
# ----------------------------------------------------------------------

# Ignore Mac OS X Finder configuration files
.DS_Store

# Thumbnails and resource fork files
.Trashes
ehthumbs.db
.Spotlight-V100
.TemporaryItems
Icon?
*~

# ----------------------------------------------------------------------
# General Build & Dependency Files (Applicable to most projects)
# ----------------------------------------------------------------------

# Virtual Environments (if inside the project folder)
venv/
env/
.venv/

# Build artifacts and compiled files
/dist/
/build/
*.o
*.a
*.so
*.lo
*.la
*.dll

#Exe
chess_engine